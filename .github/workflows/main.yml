name: main

on:
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  run-workflow:
    runs-on: ubuntu-latest

    steps:
    - name: print something
      run: |
        echo "workflow running..."
  
